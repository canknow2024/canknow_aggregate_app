# 占位符文件 - 请替换为真实的iOS发行证书

此文件是占位符，用于提醒您需要添加真实的iOS发行证书文件。

## 需要执行的操作：

1. 从 Apple Developer 网站获取 iOS Distribution 证书
2. 将证书导出为 .p12 格式（包含私钥）
3. 设置密码
4. 将文件重命名为 `ios_distribution.p12` 并替换此占位符文件
5. 在 GitHub 仓库的 Settings > Secrets and variables > Actions 中添加 `P12_PASSWORD` 环境变量，值为证书密码

## 文件要求：
- 文件名：ios_distribution.p12
- 格式：PKCS#12 (.p12)
- 内容：iOS Distribution 证书（包含私钥）
- 密码：需要设置密码并在 GitHub Secrets 中配置

## 获取证书的步骤：
1. 登录 https://developer.apple.com/account/
2. 进入 Certificates, Identifiers & Profiles
3. 创建 iOS Distribution 证书
4. 下载并安装到 Keychain Access
5. 从 Keychain Access 导出为 .p12 格式
6. 设置密码并保存

注意：此占位符文件不会影响构建，但缺少真实的证书文件会导致构建失败。 